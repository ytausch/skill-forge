# This file is auto-generated by generate.py. Do not edit manually.
context:
  version: "0.22.5"

source:
  url: https://github.com/googleworkspace/cli/archive/refs/tags/v0.22.5.tar.gz
  sha256: 1e55ec8c6ee87fac7d422975604a2d546c35f6d687a1cfaba7c7cc0d3c05663f

about:
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/googleworkspace/cli
  repository: https://github.com/googleworkspace/cli
  documentation: https://github.com/googleworkspace/cli/blob/v0.22.5/docs/skills.md

outputs:
  - package:
      name: agent-skill-gws-admin-reports
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-admin-reports
        - cp $SRC_DIR/skills/gws-admin-reports/SKILL.md $PREFIX/share/agent-skills/gws-admin-reports/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-admin-reports/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workspace Admin SDK: Audit logs and usage reports."

  - package:
      name: agent-skill-gws-calendar
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-calendar
        - cp $SRC_DIR/skills/gws-calendar/SKILL.md $PREFIX/share/agent-skills/gws-calendar/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-calendar/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Calendar: Manage calendars and events."

  - package:
      name: agent-skill-gws-calendar-agenda
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-calendar-agenda
        - cp $SRC_DIR/skills/gws-calendar-agenda/SKILL.md $PREFIX/share/agent-skills/gws-calendar-agenda/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-calendar-agenda/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Calendar: Show upcoming events across all calendars."

  - package:
      name: agent-skill-gws-calendar-insert
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-calendar-insert
        - cp $SRC_DIR/skills/gws-calendar-insert/SKILL.md $PREFIX/share/agent-skills/gws-calendar-insert/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-calendar-insert/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Calendar: Create a new event."

  - package:
      name: agent-skill-gws-chat
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-chat
        - cp $SRC_DIR/skills/gws-chat/SKILL.md $PREFIX/share/agent-skills/gws-chat/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-chat/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Chat: Manage Chat spaces and messages."

  - package:
      name: agent-skill-gws-chat-send
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-chat-send
        - cp $SRC_DIR/skills/gws-chat-send/SKILL.md $PREFIX/share/agent-skills/gws-chat-send/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-chat-send/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Chat: Send a message to a space."

  - package:
      name: agent-skill-gws-classroom
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-classroom
        - cp $SRC_DIR/skills/gws-classroom/SKILL.md $PREFIX/share/agent-skills/gws-classroom/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-classroom/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Classroom: Manage classes, rosters, and coursework."

  - package:
      name: agent-skill-gws-docs
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-docs
        - cp $SRC_DIR/skills/gws-docs/SKILL.md $PREFIX/share/agent-skills/gws-docs/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-docs/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Read and write Google Docs."

  - package:
      name: agent-skill-gws-docs-write
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-docs-write
        - cp $SRC_DIR/skills/gws-docs-write/SKILL.md $PREFIX/share/agent-skills/gws-docs-write/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-docs-write/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Docs: Append text to a document."

  - package:
      name: agent-skill-gws-drive
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-drive
        - cp $SRC_DIR/skills/gws-drive/SKILL.md $PREFIX/share/agent-skills/gws-drive/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-drive/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Drive: Manage files, folders, and shared drives."

  - package:
      name: agent-skill-gws-drive-upload
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-drive-upload
        - cp $SRC_DIR/skills/gws-drive-upload/SKILL.md $PREFIX/share/agent-skills/gws-drive-upload/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-drive-upload/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Drive: Upload a file with automatic metadata."

  - package:
      name: agent-skill-gws-events
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-events
        - cp $SRC_DIR/skills/gws-events/SKILL.md $PREFIX/share/agent-skills/gws-events/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-events/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Subscribe to Google Workspace events."

  - package:
      name: agent-skill-gws-events-renew
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-events-renew
        - cp $SRC_DIR/skills/gws-events-renew/SKILL.md $PREFIX/share/agent-skills/gws-events-renew/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-events-renew/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workspace Events: Renew/reactivate Workspace Events subscriptions."

  - package:
      name: agent-skill-gws-events-subscribe
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-events-subscribe
        - cp $SRC_DIR/skills/gws-events-subscribe/SKILL.md $PREFIX/share/agent-skills/gws-events-subscribe/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-events-subscribe/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workspace Events: Subscribe to Workspace events and stream them as NDJSON."

  - package:
      name: agent-skill-gws-forms
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-forms
        - cp $SRC_DIR/skills/gws-forms/SKILL.md $PREFIX/share/agent-skills/gws-forms/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-forms/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Read and write Google Forms."

  - package:
      name: agent-skill-gws-gmail
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail
        - cp $SRC_DIR/skills/gws-gmail/SKILL.md $PREFIX/share/agent-skills/gws-gmail/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Send, read, and manage email."

  - package:
      name: agent-skill-gws-gmail-forward
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail-forward
        - cp $SRC_DIR/skills/gws-gmail-forward/SKILL.md $PREFIX/share/agent-skills/gws-gmail-forward/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail-forward/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Forward a message to new recipients."

  - package:
      name: agent-skill-gws-gmail-read
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail-read
        - cp $SRC_DIR/skills/gws-gmail-read/SKILL.md $PREFIX/share/agent-skills/gws-gmail-read/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail-read/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Read a message and extract its body or headers."

  - package:
      name: agent-skill-gws-gmail-reply
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail-reply
        - cp $SRC_DIR/skills/gws-gmail-reply/SKILL.md $PREFIX/share/agent-skills/gws-gmail-reply/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail-reply/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Reply to a message (handles threading automatically)."

  - package:
      name: agent-skill-gws-gmail-reply-all
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail-reply-all
        - cp $SRC_DIR/skills/gws-gmail-reply-all/SKILL.md $PREFIX/share/agent-skills/gws-gmail-reply-all/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail-reply-all/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Reply-all to a message (handles threading automatically)."

  - package:
      name: agent-skill-gws-gmail-send
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail-send
        - cp $SRC_DIR/skills/gws-gmail-send/SKILL.md $PREFIX/share/agent-skills/gws-gmail-send/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail-send/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Send an email."

  - package:
      name: agent-skill-gws-gmail-triage
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail-triage
        - cp $SRC_DIR/skills/gws-gmail-triage/SKILL.md $PREFIX/share/agent-skills/gws-gmail-triage/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail-triage/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Show unread inbox summary (sender, subject, date)."

  - package:
      name: agent-skill-gws-gmail-watch
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-gmail-watch
        - cp $SRC_DIR/skills/gws-gmail-watch/SKILL.md $PREFIX/share/agent-skills/gws-gmail-watch/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-gmail-watch/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Gmail: Watch for new emails and stream them as NDJSON."

  - package:
      name: agent-skill-gws-keep
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-keep
        - cp $SRC_DIR/skills/gws-keep/SKILL.md $PREFIX/share/agent-skills/gws-keep/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-keep/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Manage Google Keep notes."

  - package:
      name: agent-skill-gws-meet
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-meet
        - cp $SRC_DIR/skills/gws-meet/SKILL.md $PREFIX/share/agent-skills/gws-meet/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-meet/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Manage Google Meet conferences."

  - package:
      name: agent-skill-gws-modelarmor
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-modelarmor
        - cp $SRC_DIR/skills/gws-modelarmor/SKILL.md $PREFIX/share/agent-skills/gws-modelarmor/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-modelarmor/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Model Armor: Filter user-generated content for safety."

  - package:
      name: agent-skill-gws-modelarmor-create-template
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-modelarmor-create-template
        - cp $SRC_DIR/skills/gws-modelarmor-create-template/SKILL.md $PREFIX/share/agent-skills/gws-modelarmor-create-template/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-modelarmor-create-template/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Model Armor: Create a new Model Armor template."

  - package:
      name: agent-skill-gws-modelarmor-sanitize-prompt
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-modelarmor-sanitize-prompt
        - cp $SRC_DIR/skills/gws-modelarmor-sanitize-prompt/SKILL.md $PREFIX/share/agent-skills/gws-modelarmor-sanitize-prompt/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-modelarmor-sanitize-prompt/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Model Armor: Sanitize a user prompt through a Model Armor template."

  - package:
      name: agent-skill-gws-modelarmor-sanitize-response
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-modelarmor-sanitize-response
        - cp $SRC_DIR/skills/gws-modelarmor-sanitize-response/SKILL.md $PREFIX/share/agent-skills/gws-modelarmor-sanitize-response/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-modelarmor-sanitize-response/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Model Armor: Sanitize a model response through a Model Armor template."

  - package:
      name: agent-skill-gws-people
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-people
        - cp $SRC_DIR/skills/gws-people/SKILL.md $PREFIX/share/agent-skills/gws-people/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-people/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google People: Manage contacts and profiles."

  - package:
      name: agent-skill-gws-script
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-script
        - cp $SRC_DIR/skills/gws-script/SKILL.md $PREFIX/share/agent-skills/gws-script/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-script/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Manage Google Apps Script projects."

  - package:
      name: agent-skill-gws-script-push
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-script-push
        - cp $SRC_DIR/skills/gws-script-push/SKILL.md $PREFIX/share/agent-skills/gws-script-push/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-script-push/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Apps Script: Upload local files to an Apps Script project."

  - package:
      name: agent-skill-gws-shared
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-shared
        - cp $SRC_DIR/skills/gws-shared/SKILL.md $PREFIX/share/agent-skills/gws-shared/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-shared/SKILL.md
          strict: true
    about:
      summary: "Agent skill: gws CLI: Shared patterns for authentication, global flags, and output formatting."

  - package:
      name: agent-skill-gws-sheets
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-sheets
        - cp $SRC_DIR/skills/gws-sheets/SKILL.md $PREFIX/share/agent-skills/gws-sheets/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-sheets/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Sheets: Read and write spreadsheets."

  - package:
      name: agent-skill-gws-sheets-append
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-sheets-append
        - cp $SRC_DIR/skills/gws-sheets-append/SKILL.md $PREFIX/share/agent-skills/gws-sheets-append/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-sheets-append/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Sheets: Append a row to a spreadsheet."

  - package:
      name: agent-skill-gws-sheets-read
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-sheets-read
        - cp $SRC_DIR/skills/gws-sheets-read/SKILL.md $PREFIX/share/agent-skills/gws-sheets-read/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-sheets-read/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Sheets: Read values from a spreadsheet."

  - package:
      name: agent-skill-gws-slides
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-slides
        - cp $SRC_DIR/skills/gws-slides/SKILL.md $PREFIX/share/agent-skills/gws-slides/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-slides/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Slides: Read and write presentations."

  - package:
      name: agent-skill-gws-tasks
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-tasks
        - cp $SRC_DIR/skills/gws-tasks/SKILL.md $PREFIX/share/agent-skills/gws-tasks/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-tasks/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Tasks: Manage task lists and tasks."

  - package:
      name: agent-skill-gws-workflow
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-workflow
        - cp $SRC_DIR/skills/gws-workflow/SKILL.md $PREFIX/share/agent-skills/gws-workflow/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-workflow/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workflow: Cross-service productivity workflows."

  - package:
      name: agent-skill-gws-workflow-email-to-task
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-workflow-email-to-task
        - cp $SRC_DIR/skills/gws-workflow-email-to-task/SKILL.md $PREFIX/share/agent-skills/gws-workflow-email-to-task/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-workflow-email-to-task/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workflow: Convert a Gmail message into a Google Tasks entry."

  - package:
      name: agent-skill-gws-workflow-file-announce
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-workflow-file-announce
        - cp $SRC_DIR/skills/gws-workflow-file-announce/SKILL.md $PREFIX/share/agent-skills/gws-workflow-file-announce/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-workflow-file-announce/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workflow: Announce a Drive file in a Chat space."

  - package:
      name: agent-skill-gws-workflow-meeting-prep
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-workflow-meeting-prep
        - cp $SRC_DIR/skills/gws-workflow-meeting-prep/SKILL.md $PREFIX/share/agent-skills/gws-workflow-meeting-prep/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-workflow-meeting-prep/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workflow: Prepare for your next meeting: agenda, attendees, and linked docs."

  - package:
      name: agent-skill-gws-workflow-standup-report
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-workflow-standup-report
        - cp $SRC_DIR/skills/gws-workflow-standup-report/SKILL.md $PREFIX/share/agent-skills/gws-workflow-standup-report/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-workflow-standup-report/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workflow: Today's meetings + open tasks as a standup summary."

  - package:
      name: agent-skill-gws-workflow-weekly-digest
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/gws-workflow-weekly-digest
        - cp $SRC_DIR/skills/gws-workflow-weekly-digest/SKILL.md $PREFIX/share/agent-skills/gws-workflow-weekly-digest/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/gws-workflow-weekly-digest/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Google Workflow: Weekly summary: this week's meetings + unread email count."

  - package:
      name: agent-skill-gws-persona-content-creator
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-content-creator
        - cp $SRC_DIR/skills/persona-content-creator/SKILL.md $PREFIX/share/agent-skills/persona-content-creator/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-slides', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-content-creator/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create, organize, and distribute content across Workspace."

  - package:
      name: agent-skill-gws-persona-customer-support
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-customer-support
        - cp $SRC_DIR/skills/persona-customer-support/SKILL.md $PREFIX/share/agent-skills/persona-customer-support/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-customer-support/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Manage customer support — track tickets, respond, escalate issues."

  - package:
      name: agent-skill-gws-persona-event-coordinator
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-event-coordinator
        - cp $SRC_DIR/skills/persona-event-coordinator/SKILL.md $PREFIX/share/agent-skills/persona-event-coordinator/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-event-coordinator/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Plan and manage events — scheduling, invitations, and logistics."

  - package:
      name: agent-skill-gws-persona-exec-assistant
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-exec-assistant
        - cp $SRC_DIR/skills/persona-exec-assistant/SKILL.md $PREFIX/share/agent-skills/persona-exec-assistant/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-exec-assistant/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Manage an executive's schedule, inbox, and communications."

  - package:
      name: agent-skill-gws-persona-hr-coordinator
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-hr-coordinator
        - cp $SRC_DIR/skills/persona-hr-coordinator/SKILL.md $PREFIX/share/agent-skills/persona-hr-coordinator/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-hr-coordinator/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Handle HR workflows — onboarding, announcements, and employee comms."

  - package:
      name: agent-skill-gws-persona-it-admin
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-it-admin
        - cp $SRC_DIR/skills/persona-it-admin/SKILL.md $PREFIX/share/agent-skills/persona-it-admin/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-it-admin/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Administer IT — monitor security and configure Workspace."

  - package:
      name: agent-skill-gws-persona-project-manager
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-project-manager
        - cp $SRC_DIR/skills/persona-project-manager/SKILL.md $PREFIX/share/agent-skills/persona-project-manager/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-project-manager/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Coordinate projects — track tasks, schedule meetings, and share docs."

  - package:
      name: agent-skill-gws-persona-researcher
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-researcher
        - cp $SRC_DIR/skills/persona-researcher/SKILL.md $PREFIX/share/agent-skills/persona-researcher/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-researcher/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Organize research — manage references, notes, and collaboration."

  - package:
      name: agent-skill-gws-persona-sales-ops
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-sales-ops
        - cp $SRC_DIR/skills/persona-sales-ops/SKILL.md $PREFIX/share/agent-skills/persona-sales-ops/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-sales-ops/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Manage sales workflows — track deals, schedule calls, client comms."

  - package:
      name: agent-skill-gws-persona-team-lead
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/persona-team-lead
        - cp $SRC_DIR/skills/persona-team-lead/SKILL.md $PREFIX/share/agent-skills/persona-team-lead/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/persona-team-lead/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Lead a team — run standups, coordinate tasks, and communicate."

  - package:
      name: agent-skill-gws-recipe-backup-sheet-as-csv
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-backup-sheet-as-csv
        - cp $SRC_DIR/skills/recipe-backup-sheet-as-csv/SKILL.md $PREFIX/share/agent-skills/recipe-backup-sheet-as-csv/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-backup-sheet-as-csv/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Export a Google Sheets spreadsheet as a CSV file for local backup or processing."

  - package:
      name: agent-skill-gws-recipe-batch-invite-to-event
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-batch-invite-to-event
        - cp $SRC_DIR/skills/recipe-batch-invite-to-event/SKILL.md $PREFIX/share/agent-skills/recipe-batch-invite-to-event/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-batch-invite-to-event/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Add a list of attendees to an existing Google Calendar event and send notifications."

  - package:
      name: agent-skill-gws-recipe-block-focus-time
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-block-focus-time
        - cp $SRC_DIR/skills/recipe-block-focus-time/SKILL.md $PREFIX/share/agent-skills/recipe-block-focus-time/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-block-focus-time/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create recurring focus time blocks on Google Calendar to protect deep work hours."

  - package:
      name: agent-skill-gws-recipe-bulk-download-folder
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-bulk-download-folder
        - cp $SRC_DIR/skills/recipe-bulk-download-folder/SKILL.md $PREFIX/share/agent-skills/recipe-bulk-download-folder/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-bulk-download-folder/SKILL.md
          strict: true
    about:
      summary: "Agent skill: List and download all files from a Google Drive folder."

  - package:
      name: agent-skill-gws-recipe-collect-form-responses
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-collect-form-responses
        - cp $SRC_DIR/skills/recipe-collect-form-responses/SKILL.md $PREFIX/share/agent-skills/recipe-collect-form-responses/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-forms', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-collect-form-responses/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Retrieve and review responses from a Google Form."

  - package:
      name: agent-skill-gws-recipe-compare-sheet-tabs
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-compare-sheet-tabs
        - cp $SRC_DIR/skills/recipe-compare-sheet-tabs/SKILL.md $PREFIX/share/agent-skills/recipe-compare-sheet-tabs/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-compare-sheet-tabs/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Read data from two tabs in a Google Sheet to compare and identify differences."

  - package:
      name: agent-skill-gws-recipe-copy-sheet-for-new-month
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-copy-sheet-for-new-month
        - cp $SRC_DIR/skills/recipe-copy-sheet-for-new-month/SKILL.md $PREFIX/share/agent-skills/recipe-copy-sheet-for-new-month/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-copy-sheet-for-new-month/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Duplicate a Google Sheets template tab for a new month of tracking."

  - package:
      name: agent-skill-gws-recipe-create-classroom-course
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-classroom-course
        - cp $SRC_DIR/skills/recipe-create-classroom-course/SKILL.md $PREFIX/share/agent-skills/recipe-create-classroom-course/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-classroom', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-classroom-course/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a Google Classroom course and invite students."

  - package:
      name: agent-skill-gws-recipe-create-doc-from-template
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-doc-from-template
        - cp $SRC_DIR/skills/recipe-create-doc-from-template/SKILL.md $PREFIX/share/agent-skills/recipe-create-doc-from-template/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-doc-from-template/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Copy a Google Docs template, fill in content, and share with collaborators."

  - package:
      name: agent-skill-gws-recipe-create-events-from-sheet
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-events-from-sheet
        - cp $SRC_DIR/skills/recipe-create-events-from-sheet/SKILL.md $PREFIX/share/agent-skills/recipe-create-events-from-sheet/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-events-from-sheet/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Read event data from a Google Sheets spreadsheet and create Google Calendar entries for each row."

  - package:
      name: agent-skill-gws-recipe-create-expense-tracker
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-expense-tracker
        - cp $SRC_DIR/skills/recipe-create-expense-tracker/SKILL.md $PREFIX/share/agent-skills/recipe-create-expense-tracker/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-expense-tracker/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Set up a Google Sheets spreadsheet for tracking expenses with headers and initial entries."

  - package:
      name: agent-skill-gws-recipe-create-feedback-form
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-feedback-form
        - cp $SRC_DIR/skills/recipe-create-feedback-form/SKILL.md $PREFIX/share/agent-skills/recipe-create-feedback-form/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-forms', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-feedback-form/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a Google Form for feedback and share it via Gmail."

  - package:
      name: agent-skill-gws-recipe-create-gmail-filter
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-gmail-filter
        - cp $SRC_DIR/skills/recipe-create-gmail-filter/SKILL.md $PREFIX/share/agent-skills/recipe-create-gmail-filter/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-gmail-filter/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a Gmail filter to automatically label, star, or categorize incoming messages."

  - package:
      name: agent-skill-gws-recipe-create-meet-space
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-meet-space
        - cp $SRC_DIR/skills/recipe-create-meet-space/SKILL.md $PREFIX/share/agent-skills/recipe-create-meet-space/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-meet', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-meet-space/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a Google Meet meeting space and share the join link."

  - package:
      name: agent-skill-gws-recipe-create-presentation
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-presentation
        - cp $SRC_DIR/skills/recipe-create-presentation/SKILL.md $PREFIX/share/agent-skills/recipe-create-presentation/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-slides', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-presentation/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a new Google Slides presentation and add initial slides."

  - package:
      name: agent-skill-gws-recipe-create-shared-drive
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-shared-drive
        - cp $SRC_DIR/skills/recipe-create-shared-drive/SKILL.md $PREFIX/share/agent-skills/recipe-create-shared-drive/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-shared-drive/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a Google Shared Drive and add members with appropriate roles."

  - package:
      name: agent-skill-gws-recipe-create-task-list
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-task-list
        - cp $SRC_DIR/skills/recipe-create-task-list/SKILL.md $PREFIX/share/agent-skills/recipe-create-task-list/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-tasks', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-task-list/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Set up a new Google Tasks list with initial tasks."

  - package:
      name: agent-skill-gws-recipe-create-vacation-responder
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-create-vacation-responder
        - cp $SRC_DIR/skills/recipe-create-vacation-responder/SKILL.md $PREFIX/share/agent-skills/recipe-create-vacation-responder/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-create-vacation-responder/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Enable a Gmail out-of-office auto-reply with a custom message and date range."

  - package:
      name: agent-skill-gws-recipe-draft-email-from-doc
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-draft-email-from-doc
        - cp $SRC_DIR/skills/recipe-draft-email-from-doc/SKILL.md $PREFIX/share/agent-skills/recipe-draft-email-from-doc/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-draft-email-from-doc/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Read content from a Google Doc and use it as the body of a Gmail message."

  - package:
      name: agent-skill-gws-recipe-email-drive-link
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-email-drive-link
        - cp $SRC_DIR/skills/recipe-email-drive-link/SKILL.md $PREFIX/share/agent-skills/recipe-email-drive-link/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-email-drive-link/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Share a Google Drive file and email the link with a message to recipients."

  - package:
      name: agent-skill-gws-recipe-find-free-time
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-find-free-time
        - cp $SRC_DIR/skills/recipe-find-free-time/SKILL.md $PREFIX/share/agent-skills/recipe-find-free-time/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-find-free-time/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Query Google Calendar free/busy status for multiple users to find a meeting slot."

  - package:
      name: agent-skill-gws-recipe-find-large-files
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-find-large-files
        - cp $SRC_DIR/skills/recipe-find-large-files/SKILL.md $PREFIX/share/agent-skills/recipe-find-large-files/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-find-large-files/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Identify large Google Drive files consuming storage quota."

  - package:
      name: agent-skill-gws-recipe-forward-labeled-emails
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-forward-labeled-emails
        - cp $SRC_DIR/skills/recipe-forward-labeled-emails/SKILL.md $PREFIX/share/agent-skills/recipe-forward-labeled-emails/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-forward-labeled-emails/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Find Gmail messages with a specific label and forward them to another address."

  - package:
      name: agent-skill-gws-recipe-generate-report-from-sheet
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-generate-report-from-sheet
        - cp $SRC_DIR/skills/recipe-generate-report-from-sheet/SKILL.md $PREFIX/share/agent-skills/recipe-generate-report-from-sheet/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-generate-report-from-sheet/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Read data from a Google Sheet and create a formatted Google Docs report."

  - package:
      name: agent-skill-gws-recipe-label-and-archive-emails
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-label-and-archive-emails
        - cp $SRC_DIR/skills/recipe-label-and-archive-emails/SKILL.md $PREFIX/share/agent-skills/recipe-label-and-archive-emails/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-label-and-archive-emails/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Apply Gmail labels to matching messages and archive them to keep your inbox clean."

  - package:
      name: agent-skill-gws-recipe-log-deal-update
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-log-deal-update
        - cp $SRC_DIR/skills/recipe-log-deal-update/SKILL.md $PREFIX/share/agent-skills/recipe-log-deal-update/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-log-deal-update/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Append a deal status update to a Google Sheets sales tracking spreadsheet."

  - package:
      name: agent-skill-gws-recipe-organize-drive-folder
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-organize-drive-folder
        - cp $SRC_DIR/skills/recipe-organize-drive-folder/SKILL.md $PREFIX/share/agent-skills/recipe-organize-drive-folder/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-organize-drive-folder/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a Google Drive folder structure and move files into the right locations."

  - package:
      name: agent-skill-gws-recipe-plan-weekly-schedule
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-plan-weekly-schedule
        - cp $SRC_DIR/skills/recipe-plan-weekly-schedule/SKILL.md $PREFIX/share/agent-skills/recipe-plan-weekly-schedule/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-plan-weekly-schedule/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Review your Google Calendar week, identify gaps, and add events to fill them."

  - package:
      name: agent-skill-gws-recipe-post-mortem-setup
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-post-mortem-setup
        - cp $SRC_DIR/skills/recipe-post-mortem-setup/SKILL.md $PREFIX/share/agent-skills/recipe-post-mortem-setup/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-post-mortem-setup/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a Google Docs post-mortem, schedule a Google Calendar review, and notify via Chat."

  - package:
      name: agent-skill-gws-recipe-reschedule-meeting
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-reschedule-meeting
        - cp $SRC_DIR/skills/recipe-reschedule-meeting/SKILL.md $PREFIX/share/agent-skills/recipe-reschedule-meeting/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-reschedule-meeting/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Move a Google Calendar event to a new time and automatically notify all attendees."

  - package:
      name: agent-skill-gws-recipe-review-meet-participants
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-review-meet-participants
        - cp $SRC_DIR/skills/recipe-review-meet-participants/SKILL.md $PREFIX/share/agent-skills/recipe-review-meet-participants/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-meet', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-review-meet-participants/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Review who attended a Google Meet conference and for how long."

  - package:
      name: agent-skill-gws-recipe-review-overdue-tasks
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-review-overdue-tasks
        - cp $SRC_DIR/skills/recipe-review-overdue-tasks/SKILL.md $PREFIX/share/agent-skills/recipe-review-overdue-tasks/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-tasks', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-review-overdue-tasks/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Find Google Tasks that are past due and need attention."

  - package:
      name: agent-skill-gws-recipe-save-email-attachments
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-save-email-attachments
        - cp $SRC_DIR/skills/recipe-save-email-attachments/SKILL.md $PREFIX/share/agent-skills/recipe-save-email-attachments/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-save-email-attachments/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Find Gmail messages with attachments and save them to a Google Drive folder."

  - package:
      name: agent-skill-gws-recipe-save-email-to-doc
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-save-email-to-doc
        - cp $SRC_DIR/skills/recipe-save-email-to-doc/SKILL.md $PREFIX/share/agent-skills/recipe-save-email-to-doc/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-save-email-to-doc/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Save a Gmail message body into a Google Doc for archival or reference."

  - package:
      name: agent-skill-gws-recipe-schedule-recurring-event
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-schedule-recurring-event
        - cp $SRC_DIR/skills/recipe-schedule-recurring-event/SKILL.md $PREFIX/share/agent-skills/recipe-schedule-recurring-event/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-schedule-recurring-event/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Create a recurring Google Calendar event with attendees."

  - package:
      name: agent-skill-gws-recipe-send-team-announcement
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-send-team-announcement
        - cp $SRC_DIR/skills/recipe-send-team-announcement/SKILL.md $PREFIX/share/agent-skills/recipe-send-team-announcement/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-chat', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-send-team-announcement/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Send a team announcement via both Gmail and a Google Chat space."

  - package:
      name: agent-skill-gws-recipe-share-doc-and-notify
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-share-doc-and-notify
        - cp $SRC_DIR/skills/recipe-share-doc-and-notify/SKILL.md $PREFIX/share/agent-skills/recipe-share-doc-and-notify/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-docs', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-gmail', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-share-doc-and-notify/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Share a Google Docs document with edit access and email collaborators the link."

  - package:
      name: agent-skill-gws-recipe-share-event-materials
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-share-event-materials
        - cp $SRC_DIR/skills/recipe-share-event-materials/SKILL.md $PREFIX/share/agent-skills/recipe-share-event-materials/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-calendar', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-share-event-materials/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Share Google Drive files with all attendees of a Google Calendar event."

  - package:
      name: agent-skill-gws-recipe-share-folder-with-team
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-share-folder-with-team
        - cp $SRC_DIR/skills/recipe-share-folder-with-team/SKILL.md $PREFIX/share/agent-skills/recipe-share-folder-with-team/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-drive', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-share-folder-with-team/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Share a Google Drive folder and all its contents with a list of collaborators."

  - package:
      name: agent-skill-gws-recipe-sync-contacts-to-sheet
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-sync-contacts-to-sheet
        - cp $SRC_DIR/skills/recipe-sync-contacts-to-sheet/SKILL.md $PREFIX/share/agent-skills/recipe-sync-contacts-to-sheet/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-people', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-sheets', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-sync-contacts-to-sheet/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Export Google Contacts directory to a Google Sheets spreadsheet."

  - package:
      name: agent-skill-gws-recipe-watch-drive-changes
      version: ${{ version }}
    build:
      noarch: generic
      script:
        - mkdir -p $PREFIX/share/agent-skills/recipe-watch-drive-changes
        - cp $SRC_DIR/skills/recipe-watch-drive-changes/SKILL.md $PREFIX/share/agent-skills/recipe-watch-drive-changes/SKILL.md
    requirements:
      run_constraints:
        - gws ==${{ version }}
      run:
        - ${{ pin_subpackage('agent-skill-gws-shared', exact=true) }}
        - ${{ pin_subpackage('agent-skill-gws-events', exact=true) }}
    tests:
      - package_contents:
          files:
            - share/agent-skills/recipe-watch-drive-changes/SKILL.md
          strict: true
    about:
      summary: "Agent skill: Subscribe to change notifications on a Google Drive file or folder."
